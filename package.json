{
  "name": "react-slice-context",
  "description": "A a lightweight, performant, proxy-based state management library for React without any external dependencies.",
  "repository": "https://github.com/abemscac/react-slice-context",
  "private": false,
  "version": "1.0.1",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "format": "prettier --check \"{**/*,*}.{ts,tsx}\"",
    "test": "vitest run"
  },
  "dependencies": {
    "react": "16.8.4"
  },
  "peerDependencies": {
    "react": ">= 16.8.4"
  },
  "devDependencies": {
    "@types/react": "16.8.4",
    "prettier": "^3.1.1",
    "typescript": "^5.2.2",
    "vitest": "^1.1.3"
  }
}
